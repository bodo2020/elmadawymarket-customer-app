# تقرير التحقق — المعداوي Flutter

تاريخ التحقق: 9 سبتمبر 2026. المصدر المرجعي: `bodo2020/suq-arabi-mobile-market` عند `5caa03569ca48f236f54f57fae5e8121f088e4bb`.

## نتائج مكتملة

- `flutter analyze`: نجح، لا مشاكل. السجل: `analyze.log`.
- `flutter test`: نجحت 9 اختبارات. السجل: `tests.log`.
- بناء Flutter Web: نجح. السجل: `web-build.log`.
- قراءة الخدمات الفعلية: نجحت قراءة الأقسام والبانرات والمجموعات وتحديد الفرع وإعداداته والكتالوج. التفاصيل: `api-smoke.json`.
- فحص المتصفح: ظهور العربية وRTL والشعار والخريطة، حفظ عنوان ضيف محلي، تحميل أقسام ومنتجات الفرع، إضافة منتج للسلة وظهور الكمية والسعر. هذه ليست تجربة دفع مكتملة.

## ما تغطيه الاختبارات

حساب وزن 250 جرام وتحويله إلى payload، أسعار ومخزون العبوات، رفض الكميات المحلية غير الصالحة، تنسيق رقم الهاتف، منع الضغط المتكرر أثناء الإرسال، الاحتفاظ بمعرّف محاولة الطلب عند نتيجة غير مؤكدة، رفض التسعير المتغير قبل الإرسال، استعادة طلب موجود دون إرساله ثانية، ومنع إرسال محاولتين متزامنتين داخل instance واحد.

اختبارات الطلب تستخدم mocks؛ لا تثبت سلوك الإنتاج أو RLS أو التزامن بين أجهزة متعددة.

## ما يحتاج اختبار قبول قبل النشر

- تسجيل حساب حقيقي وOTP واستعادة كلمة المرور وروابط العودة على الهاتف.
- شراء وخصم مخزون وقسائم ومحفظة وتتبع طلب واسترجاع ورفع صور، على مشروع اختبار أو ضمن تجربة تشغيل معتمدة.
- الكاميرا ومسح الباركود والموقع والأذونات ودورة حياة التطبيق على جهاز Android وiPhone فعليين.
- بناء وتوقيع iOS عبر macOS/Xcode، وتوقيع Android للإنتاج. إعداد release الحالي يستخدم مفتاح debug الافتراضي.
- إضافة `com.elmadawy.market://auth-callback` لقائمة روابط عودة Supabase عند تجهيز بيئة الهاتف؛ لم تتغير إعدادات المشروع الحالي.

لم ننشئ طلبات أو مرتجعات أو حسابات إنتاج، ولم نرسل SMS أثناء اختباراتنا. لم نعدّل مخطط قاعدة البيانات أو RLS أو Edge Functions.

راجع `FEATURE-MAP.md` للاختلافات الوظيفية المعروفة و`flutter_app/README.md` للتشغيل.

## تحديث التصميم — 10 سبتمبر 2026

خط Cairo مضمّن مع رخصة OFL، وألوان المصدر مثبتة بدل توليد Material للألوان. عُدّل البحث والتنقل والكروت والأزرار والكمية والدخول والعنوان ومجموعات المنتجات. فُحصت معاينة صفحة العنوان بصريًا مقابل نسخة React محلية. لا يُدّعى فحص كل شاشة وكل مقاس بكسلًا.
`flutter build apk --debug`: نجح بعد تحديث التصميم. الملف elmadawy-market-debug.apk للتجربة، ولم يتم تثبيته واختباره على هاتف فعلي. بناء الويب النهائي نجح أيضًا.

## نظام التصميم الموحد — 10 سبتمبر 2026

أصبح التطبيق يستخدم Soft Modern Material 3 بهوية المعداوي: لوحة ألوان محددة، Cairo فقط، Typography وSpacing وRadius موحدة، أزرار وحقول وكروت وتنقل وDialogs وBottom Sheets مخصصة. أزيل التدرج الأخضر الكثيف من مقدمة الرئيسية وبطاقة الحساب، وأضيفت حالات عروض واضحة، Skeleton loading، انتقالات قصيرة، زر تأكيد ثابت، وحركات خفيفة للمفضلة والسلة مع احترام إعداد تقليل الحركة. لم تتغير خدمات الدفع أو الطلبات أو المصادقة أو Supabase.

## تنفيذ خطة المراجعة الشاملة

راجع DESIGN-REVIEW.md للمقارنة قبل/بعد والمقاسات والحالات التي تم اختبارها. أضيفت اختبارات التخطيط والتعافي من الخطأ وتأجيل البحث. فحص ADB لم يجد هاتفًا أو محاكيًا؛ قياس الأداء على الجهاز ما زال غير منفذ.
النتيجة النهائية لمراجعة التصميم: 21 اختبارًا ناجحًا، والتحليل بدون مشاكل. تشمل الاختبارات الشاشة الرئيسية الكاملة والتنقل عند الخط الكبير، بالإضافة إلى فحص Tokens وRTL وحد اللمس الأدنى 44 بكسل.
